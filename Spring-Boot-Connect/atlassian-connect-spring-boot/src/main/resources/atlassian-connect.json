{
  "key": "${addon.key}",
  "baseUrl": "${addon.base-url}",
  "name": "Atlassian Connect Spring Boot (Spring Boot)",
  "authentication": {
    "type": "jwt"
  },
  "lifecycle": {
    "installed": "/installed",
    "uninstalled": "/uninstalled"
  }
}
